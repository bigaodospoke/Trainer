/**
 * Worker de sincronizacao — pokeapi
 * ---------------------------------------------------------------------------
 * Placeholder da Fase 2 (Integracao de Dados). Ver docs/ARCHITECTURE.md,
 * secao "Pipeline de Sincronizacao", para o design completo deste worker:
 * leitura incremental, grade de cron, escrita em SyncLog/DataVersion e
 * upsert idempotente nas tabelas de cache (PokemonSpecies, Move, Ability,
 * Item, TierAssignment, UsageStat).
 *
 * Execucao: npm run sync:pokeapi
 */
async function main() {
  console.log('[sync:pokeapi] ainda nao implementado — ver docs/ARCHITECTURE.md > Fase 2.');
}

main();

export {};
